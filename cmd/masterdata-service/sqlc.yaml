version: "2"
sql:
  - schema: "schema.sql"  # Your schema definition file
    queries: "./queries.sql"  # Your queries file
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "pkg/db"
        emit_interface: false
